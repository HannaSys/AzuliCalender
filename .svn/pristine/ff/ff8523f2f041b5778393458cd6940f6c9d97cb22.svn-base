using ClosedXML.Excel;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Azubi_Einsatzplaner
{
    public class Import
    {
        // Importpfad festlegen
        readonly string _importPfad = @"C:\Temp\ImportEinsatzplan.xlsx";

        // Datenbank Connection
        readonly string _gConnectStr = Properties.Settings.Default.DBConnectionString;
        readonly ADODB.Connection _conn1 = new ADODB.Connection();

        // Liest aus Exceltabelle aus und speichert die Daten in der Datenbank
        public void Importxlsx()
        {
            // Alle Eintragungen in der Importdatei abrufen
            GetLongEventsXlsx(3, 1, "Berufsschule");
            GetLongEventsXlsx(3, 6, "Ferien");
            GetDayEventsXlsx(3, 4, "Brueckentag");

        }

        // Ruft die Einträge mit einer Range ab
        private void GetLongEventsXlsx( int zeile, int spalte, string art)
        {
            XLWorkbook workBook = XLWorkbook.OpenFromTemplate(_importPfad);
            IXLWorksheet workSheet = workBook.Worksheets.First();

            // geht durch solange die Zelle nicht leer ist
            while (!workSheet.Cell(zeile, spalte).IsEmpty())
            {
                _conn1.Open(_gConnectStr);
                ADODB.Recordset rs1 = new ADODB.Recordset();

                // Leeren Datensatz erzeugen
                string anfrage = "SELECT * FROM " + art + " WHERE Anfangsdatum IS NULL;";
                rs1.Open(anfrage, _conn1, ADODB.CursorTypeEnum.adOpenForwardOnly, ADODB.LockTypeEnum.adLockOptimistic);

                // Daten in den leeren Satz schreiben
                rs1.AddNew();
                rs1.Fields["Anfangsdatum"].Value = (DateTime)workSheet.Cell(zeile, spalte).Value;
                rs1.Fields["Enddatum"].Value = (DateTime)workSheet.Cell(zeile, spalte + 1).Value;
                rs1.Fields["Bezeichnung"].Value = workSheet.Cell(zeile, spalte + 2).Value.ToString();

                // Updaten und schließen der Datenbankverbindung
                rs1.Update();
                rs1.Close();
                _conn1.Close();

                zeile++;
            }
        }

        // Ruft die Einträge mit einem Datum ab (Brückentage)
        private void GetDayEventsXlsx(int zeile, int spalte, string art)
        {
            XLWorkbook workBook = XLWorkbook.OpenFromTemplate(_importPfad);
            IXLWorksheet workSheet = workBook.Worksheets.First();

            // geht durch solange die Zelle nicht leer ist
            while (!workSheet.Cell(zeile, spalte).IsEmpty())
            {
                _conn1.Open(_gConnectStr);
                ADODB.Recordset rs1 = new ADODB.Recordset();

                // Leeren Datensatz erzeugen
                string anfrage = "SELECT * FROM " + art + " WHERE Anfangsdatum IS NULL;";
                rs1.Open(anfrage, _conn1, ADODB.CursorTypeEnum.adOpenForwardOnly, ADODB.LockTypeEnum.adLockOptimistic);

                // Daten in den leeren Satz schreiben
                rs1.AddNew();
                rs1.Fields["Anfangsdatum"].Value = (DateTime)workSheet.Cell(zeile, spalte).Value;
                rs1.Fields["Enddatum"].Value = (DateTime)workSheet.Cell(zeile, spalte).Value;

                // ist bei den Brückentagen der Turnus und bei den Ferien der Ferien 
                rs1.Fields["Bezeichnung"].Value = workSheet.Cell(zeile, spalte + 1).Value.ToString();

                // Updaten und schließen der Datenbankverbindung
                rs1.Update();
                rs1.Close();
                _conn1.Close();

                zeile++;
            }
        }
    }
}